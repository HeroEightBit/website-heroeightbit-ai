services:
  nextjs-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: heroeightbit-website
    restart: always
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production